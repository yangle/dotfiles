[include]
    path = ~/.gitconfig-whoami
[color]
    ui=auto
[color "branch"]
    current = blue reverse
    local = red
    remote = green
[color "diff"]
    meta = blue bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[core]
    excludesfile = ~/.gitignore
    editor = vim
[push]
    default = simple
[grep]
    lineNumber = true
[merge]
    ff = only
