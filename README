dotfiles

"setup" deploys _* and -* files to $HOME folder:
    _xxx => ~/.xxx
    _yyy/ => ~/.yyy/
    -zzz/.../aaa => ~/.zzz/.../aaa   -- leaving other files under ~/.zzz out of git
    uuu => ignored

make sure all submodules are fetched: git submodule update --init --recursive

dconf: basic gnome settings
